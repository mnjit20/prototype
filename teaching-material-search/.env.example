PORT=3000
NODE_ENV=development

# Local Docker default. Most hosted Postgres providers expose a DATABASE_URL too.
DATABASE_URL=postgres://postgres:password@localhost:5432/teaching_materials
DB_SSL=false

# Replace this with the 10k challenge file path when seeding the full dataset.
DATA_FILE=./data/teaching-materials.json
DEFAULT_SEARCH_LIMIT=20
